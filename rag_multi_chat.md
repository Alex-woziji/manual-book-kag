# 本地大模型 Chatbot 多轮对话系统架构

## 1. 系统概述

本系统是一个基于本地大模型的智能对话机器人（Chatbot），旨在提供流畅、智能的多轮对话体验。系统核心功能包括数据预处理、意图识别、对话接口（集成后台数据存储）、向量入库接口以及历史对话管理。
通过模块化的设计，系统能够高效处理用户输入，理解用户意图，并结合上下文进行有意义的回复。

## 2. 核心功能模块

### 2.1 数据预处理
- **功能描述**：对用户输入的原始文本进行清洗和规范化，为后续的意图识别和模型处理做准备。
- **主要处理**：
    - 文本清洗和处理： 将pdf或者word、txt等文档进行文本块划分并向量化
    - 多模态（可选）：图片，视频或音频处理的描述、向量化处理
    - pdf或word文档中媒体处理（可选）：对pdf或word中的图片个是进行ocr识别处理

### 2.2 意图识别
- **功能描述**：分析用户输入，判断其真实的对话意图。
- **主要处理**：
    - 基于规则或模型的意图分类：将用户输入映射到预定义的意图类别（如：问候、查询天气、预订服务、闲聊等）。
    - 意图参数提取：从用户输入中提取与意图相关的参数（槽位填充）。


### 2.3 对话接口（后台数据存储）
- **功能描述**：提供核心的对话交互能力，接收用户消息，调用大模型生成回复，并管理对话状态。后台负责存储所有对话数据。
- **主要接口**（详见 `multi_chat.md`）：
    - **聊天对话接口 (`POST /chat/message`)**:
        - 接收用户 `user_id`、`session_id`（首次可空）和 `message`。
        - 系统处理用户消息，调用大模型生成回复。
        - 返回 `session_id`、大模型生成的 `answer` 以及当前会话的 `history`。
        - 后台自动记录每轮对话的用户输入和模型输出。
    - **获取历史对话接口 (`POST /chat/history`)**:
        - 接收用户 `user_id` 和 `session_id`。
        - 从后台数据存储中检索并返回指定会话的完整历史对话内容。
- **后台数据存储**：
    - 存储用户基本信息（`user_id`）。
    - 存储会话信息（`session_id`，关联到 `user_id`）。
    - 存储每一轮的对话内容（角色：user/assistant，消息内容，时间戳等）。
    - 数据库选型可考虑关系型数据库（如 PostgreSQL, MySQL）或 NoSQL 数据库（如 MongoDB），根据实际需求选择。

### 2.4 向量入库接口
- **功能描述**：将文本数据（如知识库文档、历史对话、FAQ 等）转换为向量表示，并存储到向量数据库中，以便进行高效的语义相似度搜索。
- **主要处理**：
    - 文本切分：将长文本切分为合适的片段。
    - 向量化：使用预训练的 Embedding 模型（或大模型自身）将文本片段转换为高维向量。
    - 向量存储：将文本片段及其对应的向量存储到向量数据库（如 Milvus, FAISS, Pinecone, ChromaDB 等）。
    - 元数据管理：同时存储与文本片段相关的元数据（如来源、类别、时间戳等）。
- **接口形式**（示例）：
    - `POST /vector/add_documents`：接收文本数据，进行向量化并入库。
    - `POST /vector/search`：接收查询文本，返回语义相似的文档片段。

### 2.5 历史对话管理
- **功能描述**：有效管理用户的历史对话数据，支持多轮对话的上下文理解，并确保数据安全和隐私。
- **主要处理**：
    - **上下文维护**：在 `POST /chat/message` 接口调用时，根据 `session_id` 加载相关历史对话，作为上下文信息传递给大模型。
    - **会话管理**：
        - `session_id` 作为会话的唯一标识。首次对话时，若 `session_id` 为空，系统会自动生成并返回。
        - 支持会话的创建、延续和结束。
    - **历史记录查询**：通过 `POST /chat/history` 接口，用户可以查询自己特定会话的历史记录。
    - **数据安全与隐私**：用户只能访问自己的会话历史（通过 `user_id` 控制）。


